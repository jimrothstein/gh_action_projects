name:  205 rocker 

## USE  rocker,  but no git?, otherwise works
run-name: 205 dependencies

on:
  workflow_dispatch:
#  pull_request:

jobs:
  import-data:
    runs-on: ubuntu-latest
    container:
      image: rocker/r-ver:latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name:  install binary pak - WoRKS!
        run:  Rscript -e 'install.packages("pak",
           repos = sprintf("https://r-lib.github.io/p/pak/stable/%s/%s/%s",
           .Platform$pkgType, R.Version()$os, R.Version()$arch))'

      - name:  install diffdf binary
        run:  Rscript -e 'pak::pkg_install("diffdf")'  # default is binary


      - name:  install git (sudo requires no passwd)
        run:  sudo apt-get install git -y

#      - name: Set up R
#        if: ${{ !env.ACT }}  # do not on act
#        uses: r-lib/actions/setup-r@v2
#        with:
#          r-version: 'latest'
#          use-public-rspm: true
#
#      - name: Install packages
#        if: ${{ !env.ACT }}
#        uses: r-lib/actions/setup-r-dependencies@v2
#        with:
#          dependencies: hard


      - name: Run script
        run: Rscript -e 'source("R/01_first.R")'

      - name: Commit results
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git commit -m 'Data updated' || echo "No changes to commit"
          git push origin || echo "No changes to commit"
